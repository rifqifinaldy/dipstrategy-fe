import styled from "styled-components";

export const ContactFormGroup = styled.form`
  padding: 40px 40px;
  border-radius: 5px;
  width: 100%;
  background-color: ${({ theme }) => theme.colors.black};
`;
